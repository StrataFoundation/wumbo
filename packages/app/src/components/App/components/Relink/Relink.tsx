import React from "react";
import { Relink } from "wumbo-common";
import { AppContainer } from "../common/AppContainer";

export const RelinkRoute: React.FC = () => {
  return (
    <AppContainer>
      <Relink />
    </AppContainer>
  );
};
